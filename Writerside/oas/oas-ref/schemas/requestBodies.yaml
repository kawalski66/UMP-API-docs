requestBodies:
  aircraftIdBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas.yaml#/schemas/aircraft"

  transmitterBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas.yaml#/schemas/transmitter"

  transponderBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas.yaml#/schemas/transponder"
      description: "Create or replace an aircraft transponder"

  sensorBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas.yaml#/schemas/sensor"
      description: "Create or replace a sensor resource."

  productBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "./schemas.yaml#/schemas/product"
      description: "Fetch list of product."
