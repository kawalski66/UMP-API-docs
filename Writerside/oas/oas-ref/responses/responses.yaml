responses:
  SuccessAircraft:
    description: "Success"
    content:
      application/json:
        schema:
          type: "array"
          items:
            $ref: "../schemas/schemas.yaml#/schemas/aircraft"

  SuccessAircraftId:
    description: "Success"
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/aircraft"