responses:
  SuccessAircraft:
    description: "Success"
    content:
      application/json:
        schema:
          type: "array"
          items:
            $ref: "../schemas/schemas.yaml#/schemas/aircraft"

  SuccessAircraftId:
    description: "Success"
    content:
      application/json:
        schema:
          $ref: "../schemas/schemas.yaml#/schemas/aircraft"

  CreatedAircraftId:
    description: "Created"
    content:
      application/json:
        schema:
          $ref: "../schemas/schemas.yaml#/schemas/aircraft"

  Deleted:
    description: "Deleted"
